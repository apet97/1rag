# Configuration (deprecated summary)

This file is kept only as a pointer. For current defaults, overrides, and copy/paste commands, use:
- `docs/CONFIGURATION.md` – canonical config matrix
- `README.md` and `RUN_ON_WORK_MAC.md` – zero-config setup for the work Mac (VPN defaults)

Legacy details previously here are out of date. Please consult the files above.
